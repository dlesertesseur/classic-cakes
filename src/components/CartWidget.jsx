import React from "react";

const CartWidget = () => {
  return (
    <div>
      <a className="nav-item" href="cart">
        <img className="nav-logo" src="/basket.png" alt="logo" />
      </a>
    </div>
  );
};

export default CartWidget;
